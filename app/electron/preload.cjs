/**
 * Inferno Terminal — Preload script
 *
 * Uses the generic libcss/desktop scaffold.
 */
const { createPreload } = require('../src/lib/libcss/desktop/createPreload.cjs');

createPreload({
  name: 'Inferno Terminal',
  version: '2.0.0',
});
